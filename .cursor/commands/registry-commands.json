{
  "version": "1.0.0",
  "commands": [
    {
      "id": "registry.upload",
      "title": "Upload to Registries",
      "description": "Upload all components to configured registries",
      "command": "./scripts/upload/upload-to-registries.sh",
      "args": [],
      "options": {
        "cwd": "${workspaceFolder}",
        "group": "registry",
        "icon": "📤",
        "category": "Registry"
      }
    },
    {
      "id": "registry.validate",
      "title": "Validate Packages",
      "description": "Validate uploaded packages can be consumed",
      "command": "./scripts/validation/validate-packages.sh",
      "args": [],
      "options": {
        "cwd": "${workspaceFolder}",
        "group": "registry",
        "icon": "✅",
        "category": "Registry"
      }
    },
    {
      "id": "registry.list-remotes",
      "title": "List Remotes",
      "description": "List configured Conan remotes",
      "command": "conan remote list",
      "args": [],
      "options": {
        "cwd": "${workspaceFolder}",
        "group": "registry",
        "icon": "📋",
        "category": "Registry"
      }
    },
    {
      "id": "registry.clean-cache",
      "title": "Clean Cache",
      "description": "Clean local Conan cache",
      "command": "conan cache clean '*'",
      "args": [],
      "options": {
        "cwd": "${workspaceFolder}",
        "group": "registry",
        "icon": "🧹",
        "category": "Registry"
      }
    },
    {
      "id": "registry.login-artifactory",
      "title": "Login to Artifactory",
      "description": "Login to Artifactory remote",
      "command": "conan remote login artifactory $ARTIFACTORY_USER -p $ARTIFACTORY_TOKEN",
      "args": [],
      "options": {
        "cwd": "${workspaceFolder}",
        "group": "registry",
        "icon": "🔑",
        "category": "Registry"
      }
    }
  ]
}
